/**
 * src/lib/demo-session.ts
 *
 * Temporary: hardcoded demo student until auth is implemented.
 * All practice flow uses this student ID for DB operations.
 * Replace with session/auth lookup when auth is added.
 */

// Matches seed.ts: ID.students.demo
export const DEMO_STUDENT_ID = "student-seed-demo";
